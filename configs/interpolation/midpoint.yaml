# @package interpolation
enabled: true
type: midpoint  # Midpoint evaluation for 2+ models
steps: 1  # Ignored for midpoint evaluation
save_every: null  # Run interpolation every N epochs (null = only at end)
